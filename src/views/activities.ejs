<%- include('partials/header', { pageTitle, user, currentPath, flash }) %>

<section class="page-header">
  <div>
    <span class="eyebrow">Register</span>
    <h1>RoPA register</h1>
    <p>Review processing activities as expandable records, then open full detail when needed.</p>
  </div>
  <div class="header-actions">
    <button class="button secondary" type="button" data-search-toggle aria-expanded="<%= activeFilters.length ? 'true' : 'false' %>">Search</button>
    <a class="button" href="/activities/new">New activity</a>
  </div>
</section>

<%
  const roleTabs = [
    { href: '/activities', title: 'All activities', active: !filters.futurewhiz_role },
    {
      href: '/activities?futurewhiz_role=controller',
      title: 'Futurewhiz as controller',
      active: filters.futurewhiz_role === 'controller'
    },
    {
      href: '/activities?futurewhiz_role=processor',
      title: 'Futurewhiz as processor',
      active: filters.futurewhiz_role === 'processor'
    }
  ];
%>

<section class="role-tab-strip">
  <% roleTabs.forEach((tab) => { %>
    <a class="role-tab <%= tab.active ? 'active' : '' %>" href="<%= tab.href %>"><%= tab.title %></a>
  <% }) %>
</section>

<section class="panel search-panel <%= activeFilters.length ? 'is-open' : '' %>" data-search-panel <%= activeFilters.length ? '' : 'hidden' %>>
  <div class="register-toolbar">
    <div>
      <h2>Search and filters</h2>
      <p>Expand the full filter set only when you need it.</p>
    </div>
    <div class="header-actions">
      <a class="button secondary" href="/exports/register.pdf<%= exportQuery ? `?${exportQuery}` : '' %>">Export PDF</a>
      <a class="button secondary" href="/exports/register.xls<%= exportQuery ? `?${exportQuery}` : '' %>">Export Excel</a>
    </div>
  </div>

  <form class="filters-grid" method="get" action="/activities">
    <input type="hidden" name="futurewhiz_role" value="<%= filters.futurewhiz_role %>" />
    <label class="span-2">
      <span>Keyword search</span>
      <input type="text" name="q" value="<%= filters.q %>" placeholder="Search activity, purpose, vendor, security measures, or reference" />
    </label>

    <label>
      <span>Vendor</span>
      <select name="vendor">
        <option value="">All vendors</option>
        <% filterOptions.vendors.forEach((vendor) => { %>
          <option value="<%= vendor %>" <%= filters.vendor === vendor ? 'selected' : '' %>><%= vendor %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Product / service</span>
      <select name="product">
        <option value="">All products</option>
        <% filterOptions.products.forEach((product) => { %>
          <option value="<%= product %>" <%= filters.product === product ? 'selected' : '' %>><%= product %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Department</span>
      <select name="department">
        <option value="">All departments</option>
        <% filterOptions.departments.forEach((department) => { %>
          <option value="<%= department %>" <%= filters.department === department ? 'selected' : '' %>><%= department %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Business owner</span>
      <select name="owner">
        <option value="">All owners</option>
        <% filterOptions.owners.forEach((owner) => { %>
          <option value="<%= owner.email %>" <%= filters.owner === owner.email ? 'selected' : '' %>><%= owner.name %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Status</span>
      <select name="status">
        <option value="">All statuses</option>
        <% ['draft','pending_business_input','pending_legal_review','approved','needs_update','archived'].forEach((statusValue) => { %>
          <option value="<%= statusValue %>" <%= filters.status === statusValue ? 'selected' : '' %>><%= statusLabel(statusValue) %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Lawful basis</span>
      <select name="lawful_basis">
        <option value="">All lawful bases</option>
        <% filterOptions.lawfulBases.forEach((lawfulBasis) => { %>
          <option value="<%= lawfulBasis %>" <%= filters.lawful_basis === lawfulBasis ? 'selected' : '' %>><%= lawfulBasis %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Children’s data</span>
      <select name="children_data">
        <% booleanFilters.forEach((option) => { %>
          <option value="<%= option.value %>" <%= filters.children_data === option.value ? 'selected' : '' %>><%= option.label %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Special category data</span>
      <select name="special_category_data">
        <% booleanFilters.forEach((option) => { %>
          <option value="<%= option.value %>" <%= filters.special_category_data === option.value ? 'selected' : '' %>><%= option.label %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>International transfers</span>
      <select name="international_transfers">
        <% booleanFilters.forEach((option) => { %>
          <option value="<%= option.value %>" <%= filters.international_transfers === option.value ? 'selected' : '' %>><%= option.label %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>AI involvement</span>
      <select name="ai_involvement">
        <% booleanFilters.forEach((option) => { %>
          <option value="<%= option.value %>" <%= filters.ai_involvement === option.value ? 'selected' : '' %>><%= option.label %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Overdue review</span>
      <select name="overdue_review">
        <% booleanFilters.forEach((option) => { %>
          <option value="<%= option.value %>" <%= filters.overdue_review === option.value ? 'selected' : '' %>><%= option.label %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Missing linked items</span>
      <select name="missing_links">
        <option value="">Any</option>
        <option value="missing_dpia" <%= filters.missing_links === 'missing_dpia' ? 'selected' : '' %>>Missing DPIA</option>
        <option value="missing_vendor_review" <%= filters.missing_links === 'missing_vendor_review' ? 'selected' : '' %>>Missing vendor review / DPA</option>
        <option value="missing_security_review" <%= filters.missing_links === 'missing_security_review' ? 'selected' : '' %>>Missing security review</option>
        <option value="missing_any_core_link" <%= filters.missing_links === 'missing_any_core_link' ? 'selected' : '' %>>Missing any core linked item</option>
      </select>
    </label>

    <label>
      <span>Retention field</span>
      <select name="retention_state">
        <option value="all" <%= filters.retention_state === 'all' ? 'selected' : '' %>>Any</option>
        <option value="present" <%= filters.retention_state === 'present' ? 'selected' : '' %>>Retention present</option>
        <option value="missing" <%= filters.retention_state === 'missing' ? 'selected' : '' %>>Retention missing</option>
      </select>
    </label>

    <label>
      <span>Retention period contains</span>
      <input type="text" name="retention_search" value="<%= filters.retention_search %>" placeholder="e.g. 12 months" />
    </label>

    <label>
      <span>Security measures field</span>
      <select name="missing_security_measures">
        <% booleanFilters.forEach((option) => { %>
          <option value="<%= option.value %>" <%= filters.missing_security_measures === option.value ? 'selected' : '' %>><%= option.label %></option>
        <% }) %>
      </select>
    </label>

    <label>
      <span>Sort by</span>
      <select name="sort">
        <option value="next_review_asc" <%= filters.sort === 'next_review_asc' ? 'selected' : '' %>>Next review date</option>
        <option value="updated_desc" <%= filters.sort === 'updated_desc' ? 'selected' : '' %>>Last updated</option>
        <option value="activity_name_asc" <%= filters.sort === 'activity_name_asc' ? 'selected' : '' %>>Activity name</option>
        <option value="status_asc" <%= filters.sort === 'status_asc' ? 'selected' : '' %>>Status</option>
      </select>
    </label>

    <div class="filter-actions span-2">
      <button class="button" type="submit">Apply filters</button>
      <a class="button ghost" href="<%= filters.futurewhiz_role ? `/activities?futurewhiz_role=${filters.futurewhiz_role}` : '/activities' %>">Reset</a>
    </div>
  </form>
</section>

<section class="register-card-list">
  <% if (activeFilters.length) { %>
    <div class="active-filter-list">
      <% activeFilters.forEach((filter) => { %>
        <span class="filter-chip"><%= filter %></span>
      <% }) %>
    </div>
  <% } %>

  <div class="register-toolbar">
    <div>
      <h2>Processing activities</h2>
      <p><strong><%= resultCount %></strong> matching activities.</p>
    </div>
  </div>

  <% if (activities.length === 0) { %>
    <section class="panel">
      <p class="muted">No activities matched the selected filters.</p>
    </section>
  <% } %>

  <% activities.forEach((activity) => { %>
    <details class="register-card panel">
      <summary class="register-card-summary">
        <div class="register-card-primary">
          <strong><%= activity.activity_name %></strong>
          <span class="muted"><%= activity.reference_code %></span>
        </div>
        <div class="register-card-meta">
          <span><%= activity.department || 'No team set' %></span>
          <span class="line-clamp-2"><%= activity.purpose_of_processing || 'No purpose recorded' %></span>
        </div>
        <div class="register-card-status">
          <span class="status-pill <%= statusClass(activity.status) %>"><%= statusLabel(activity.status) %></span>
        </div>
      </summary>

      <div class="register-card-body">
        <section class="detail-grid compact-grid">
          <article class="panel inset-panel">
            <div class="section-heading">
              <div><h2>Ownership and accountability</h2></div>
            </div>
            <dl class="detail-list">
              <div><dt>Futurewhiz role</dt><dd><span class="role-badge <%= activity.futurewhiz_role %>"><%= futurewhizRoleLabel(activity.futurewhiz_role) %></span></dd></div>
              <div><dt>Controller</dt><dd><%= activity.futurewhiz_role === 'processor' ? (activity.controller_name || 'Not documented') : (controllerProfile.company_name || 'Not set') %> <span class="muted"><%= activity.futurewhiz_role === 'processor' ? (activity.controller_contact_details || '') : (controllerProfileContactDetails(controllerProfile) || '') %></span></dd></div>
              <div><dt>Business owner</dt><dd><%= activity.business_owner_name %> <span class="muted"><%= activity.business_owner_email %></span></dd></div>
              <div><dt>Legal reviewer</dt><dd><%= activity.legal_reviewer_name || 'Not assigned' %> <span class="muted"><%= activity.legal_reviewer_email %></span></dd></div>
              <div><dt>Last updated</dt><dd><%= formatDateTime(activity.last_updated_at) %></dd></div>
              <div><dt>Next review</dt><dd><%= activity.next_review_date ? formatDate(activity.next_review_date) : 'Not set' %> <% if (activity.is_overdue) { %><span class="flag danger">Overdue</span><% } %></dd></div>
            </dl>
          </article>

          <article class="panel inset-panel security-panel">
            <div class="section-heading">
              <div><h2>Security measures</h2></div>
            </div>
            <div class="rich-text"><%= activity.security_measures || 'Missing security measures' %></div>
          </article>
        </section>

        <section class="detail-grid compact-grid">
          <article class="panel inset-panel">
            <h2>Processing details</h2>
            <dl class="detail-list">
              <div><dt>Short description</dt><dd><%= activity.short_description || 'Not set' %></dd></div>
              <div><dt>Product / service</dt><dd><%= activity.product_service || 'Not set' %></dd></div>
              <div><dt>Lawful basis</dt><dd><%= activity.lawful_basis || 'Not set' %></dd></div>
              <div><dt>Data subjects</dt><dd><%= activity.data_subject_categories.length ? activity.data_subject_categories.join(', ') : 'Not set' %></dd></div>
              <div><dt>Personal data</dt><dd><%= activity.personal_data_categories.length ? activity.personal_data_categories.join(', ') : 'Not set' %></dd></div>
              <div><dt>Recipients</dt><dd><%= activity.recipient_categories.length ? activity.recipient_categories.join(', ') : 'Not set' %></dd></div>
              <div><dt>Processors / vendors</dt><dd><%= activity.processors_vendors.length ? activity.processors_vendors.join(', ') : 'Not set' %></dd></div>
              <div><dt>Retention period</dt><dd><%= activity.retention_period || 'Not set' %></dd></div>
            </dl>
          </article>

          <article class="panel inset-panel">
            <h2>Linked compliance items</h2>
            <dl class="detail-list">
              <div><dt>Vendor review / DPA</dt><dd><%= activity.vendor_review_ref || 'Missing' %></dd></div>
              <div><dt>DPIA</dt><dd><%= activity.dpia_ref || 'Missing' %></dd></div>
              <div><dt>LIA</dt><dd><%= activity.lia_ref || 'Not linked' %></dd></div>
              <div><dt>Privacy notice</dt><dd><%= activity.privacy_notice_ref || 'Not linked' %></dd></div>
              <div><dt>Security review</dt><dd><%= activity.security_review_ref || 'Missing' %></dd></div>
              <div><dt>AI tool review</dt><dd><%= activity.ai_tool_review_ref || 'Not linked' %></dd></div>
            </dl>
          </article>
        </section>

        <div class="register-card-actions">
          <a class="button secondary" href="/activities/<%= activity.id %>">Open full detail</a>
          <a class="button ghost" href="/activities/<%= activity.id %>/edit">Edit</a>
          <a class="button ghost" href="/activities/<%= activity.id %>/export.pdf">Export PDF</a>
        </div>
      </div>
    </details>
  <% }) %>
</section>

<script>
  const searchToggle = document.querySelector('[data-search-toggle]');
  const searchPanel = document.querySelector('[data-search-panel]');

  if (searchToggle && searchPanel) {
    searchToggle.addEventListener('click', () => {
      const isHidden = searchPanel.hasAttribute('hidden');
      if (isHidden) {
        searchPanel.removeAttribute('hidden');
        searchPanel.classList.add('is-open');
        searchToggle.setAttribute('aria-expanded', 'true');
      } else {
        searchPanel.setAttribute('hidden', '');
        searchPanel.classList.remove('is-open');
        searchToggle.setAttribute('aria-expanded', 'false');
      }
    });
  }
</script>

<%- include('partials/footer') %>
