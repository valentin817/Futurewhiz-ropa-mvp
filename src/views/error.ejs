<%- include('partials/header', { pageTitle: 'Error', user, currentPath, flash }) %>

<section class="panel narrow-panel">
  <h1>Something needs attention</h1>
  <p><%= message %></p>
  <a class="button" href="<%= user ? '/dashboard' : '/login' %>">Back</a>
</section>

<%- include('partials/footer') %>
